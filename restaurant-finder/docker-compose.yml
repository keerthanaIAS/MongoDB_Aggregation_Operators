version: '3.8'

services:
  mongodb:
    image: mongo:7.0
    container_name: mongodb-geo
    restart: always
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    command: mongod --bind_ip_all

volumes:
  mongodb_data: